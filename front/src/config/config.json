{
    "config":
    {
        "regex":
        {
            "username":
            {
                "pattern": "^[a-zA-Z0-9_\\-]{3,20}$",
                "description": "Username must be 3-20 characters long, and can include letters, numbers, underscores, and dashes."
            },
            "email":
            {
                "pattern": "^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$",
                "description": "Email must follow the standard email format, e.g., example@domain.com."
            },
            "password":
            {
                "pattern": "^(?=.*[A-Za-z])(?=.*\\d)(?=.*[@$!%*#?&])[A-Za-z\\d@$!%*#?&]{8,}$",
                "description": "Password must be at least 8 characters long, include at least one letter, one number, and one special character."
            }
        }
    }
}